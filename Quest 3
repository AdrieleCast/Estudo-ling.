/Crie uma função que receba um número inteiro e retorne 1 se ele for par, ou 0 se for ímpar./

#include <stdio.h>

int num(int a){
    if(a % 2 == 0){
        return 1;
    }else{
        return 0;
    }
}

int main(){
  
    int x, result;
  
    scanf("%d", &x);
     
    result = num(x);

    printf("%d", result);

    return 0;
}
