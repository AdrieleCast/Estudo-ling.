/Leia 5 números inteiros em um vetor e mostre a SOMA dos elementos./

#include <stdio.h>

int main(){

int v[5], i, soma = 0;

for(i = 0; i < 5; i++) {
scanf("%d", &v[i]);
}

for(i = 0; i < 5; i++){
soma = soma + v[i];

}

printf("%d", soma);

return 0;
}
